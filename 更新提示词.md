请在当前仓库的 security-hardening 分支上同步原作者更新。

执行前先阅读：
1. docs/FORK_BRANCH_SYNC_GUIDE.md
2. docs/SECURITY_HARDENING_GUIDE.md
3. docs/PROJECT_MEMORY.md
4. docs/TASK_LOG.md

要求：
- 先运行 git status --short --branch、git remote -v、git branch -vv。
- 确认当前分支是 security-hardening；如果不是，请切换过去。
- 从 upstream/main 同步更新：git fetch upstream，然后 git merge upstream/main。
- 如果 upstream/main 不存在，再检查 upstream/master。
- 如果有冲突，不要用 git reset --hard，不要丢弃本地安全加固改动。
- 冲突解决时必须保留安全加固不变量：Playwright cookie 默认不持久化；只有 LAST30DAYS_CN_PERSIST_COOKIES=1 时才读写 cookies；cookie 目录尽量 chmod 0700，文件尽量 chmod 0600；setup 默认不得自动安装外部工具。
- 解决后运行最小验证，至少包括相关 crawler/setup 测试或可用的针对性语法检查。
- 最后汇总 git status --short --branch、git remote -v、git branch -vv，以及验证结果。
- 不要创建 PR。